{
  "name": "@makibm/gites",
  "version": "1.0.0",
  "description": "Two-track git workflow for batching and timing client commits",
  "type": "module",
  "bin": {
    "gites": "./dist/bin/gites.js"
  },
  "files": [
    "dist",
    "hooks"
  ],
  "engines": {
    "node": ">=20"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/MakiBM/makibm-tools.git",
    "directory": "packages/gites"
  },
  "author": "Bartek Makos",
  "license": "MIT",
  "scripts": {
    "build": "rm -rf dist && tsc -p tsconfig.build.json && chmod +x dist/bin/gites.js",
    "dev": "tsx bin/gites.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "demo": "tsx scripts/demo.ts",
    "demo:reset": "tsx scripts/demo.ts --reset",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "@inquirer/prompts": "^7.2.0",
    "@makibm/cli-kit": "workspace:^",
    "picocolors": "^1.1.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
